<section class="hero-section" id="home">
  <div class="hero container">
    <div class="hero__content animate-on-scroll animate-fade-right">
      <span class="hero__badge">🚀 {{ section.settings.badge_text | default: 'Yeni Teknolojiler' }}</span>
      <h1 class="hero__title">{{ section.settings.title | default: 'Yenilikçi Teknoloji Çözümleri' }}</h1>
      <p class="hero__subtitle">{{ section.settings.subtitle | default: 'İşletmeleri dijital çağ için en son teknoloji ve yenilikçi çözümlerle güçlendiriyoruz.' }}</p>
      <div class="hero__actions">
        <a href="#products" class="button button--primary hover-lift">{{ section.settings.cta_text | default: 'Ürünleri Keşfet' }}</a>
        <a href="#contact" class="button button--secondary hover-lift">{{ section.settings.cta_secondary_text | default: 'Hemen Başla' }}</a>
      </div>
      
      {% if section.settings.show_features %}
      <div class="hero__features">
        <div class="hero__feature">
          <span class="hero__feature-icon">✓</span>
          <span>{{ section.settings.feature_1 | default: 'Ücretsiz Kargo' }}</span>
        </div>
        <div class="hero__feature">
          <span class="hero__feature-icon">✓</span>
          <span>{{ section.settings.feature_2 | default: '30 Gün İade' }}</span>
        </div>
        <div class="hero__feature">
          <span class="hero__feature-icon">✓</span>
          <span>{{ section.settings.feature_3 | default: 'Güvenli Ödeme' }}</span>
        </div>
      </div>
      {% endif %}
    </div>
    
    <div class="hero__image animate-on-scroll animate-fade-left">
      {% if section.settings.image %}
        <img src="{{ section.settings.image | image_url: width: 800 }}" alt="{{ section.settings.title }}" class="hero__main-image">
      {% else %}
        <div class="hero__placeholder float">
          <svg width="600" height="400" viewBox="0 0 600 400" fill="none" xmlns="http://www.w3.org/2000/svg">
            <defs>
              <linearGradient id="heroGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" style="stop-color:var(--color-primary);stop-opacity:0.3" />
                <stop offset="100%" style="stop-color:var(--color-primary);stop-opacity:0.1" />
              </linearGradient>
            </defs>
            <rect width="600" height="400" fill="url(#heroGradient)"/>
            <circle cx="300" cy="200" r="80" fill="var(--color-primary)" opacity="0.2"/>
            <circle cx="400" cy="150" r="40" fill="var(--color-primary)" opacity="0.15"/>
            <circle cx="200" cy="250" r="50" fill="var(--color-primary)" opacity="0.1"/>
          </svg>
        </div>
      {% endif %}
      
      <div class="hero__decoration hero__decoration--1"></div>
      <div class="hero__decoration hero__decoration--2"></div>
    </div>
  </div>
  
  <div class="hero__scroll-indicator">
    <span>Aşağı Kaydır</span>
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
      <path d="M12 5L12 19M12 19L19 12M12 19L5 12" stroke="currentColor" stroke-width="2"/>
    </svg>
  </div>
</section>

{% schema %}
{
  "name": "Hero",
  "settings": [
    {
      "type": "text",
      "id": "badge_text",
      "label": "Rozet Metni",
      "default": "Yeni Teknolojiler"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Başlık",
      "default": "Yenilikçi Teknoloji Çözümleri"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Alt Başlık",
      "default": "İşletmeleri dijital çağ için en son teknoloji ve yenilikçi çözümlerle güçlendiriyoruz."
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Birincil Buton Metni",
      "default": "Ürünleri Keşfet"
    },
    {
      "type": "text",
      "id": "cta_secondary_text",
      "label": "İkincil Buton Metni",
      "default": "Hemen Başla"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Hero Görseli"
    },
    {
      "type": "checkbox",
      "id": "show_features",
      "label": "Özellikleri Göster",
      "default": true
    },
    {
      "type": "text",
      "id": "feature_1",
      "label": "Özellik 1",
      "default": "Ücretsiz Kargo"
    },
    {
      "type": "text",
      "id": "feature_2",
      "label": "Özellik 2",
      "default": "30 Gün İade"
    },
    {
      "type": "text",
      "id": "feature_3",
      "label": "Özellik 3",
      "default": "Güvenli Ödeme"
    }
  ],
  "presets": [
    {
      "name": "Hero"
    }
  ]
}
{% endschema %}

